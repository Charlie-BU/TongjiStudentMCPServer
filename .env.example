# 与 TongjiStudentAgent 使用同一 PostgreSQL 数据库；先手动执行 sql/user_luckin_credentials.sql。
# 进程环境变量优先于 .env，连接串特殊字符须 URL 编码。
POSTGRES_DSN=postgres://<username>:<password>@<db-host>:5432/tongji_student?sslmode=disable
