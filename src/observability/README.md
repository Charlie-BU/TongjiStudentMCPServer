# Observability

TODO: add structured metadata-only logs, traces and per-tool latency/error
metrics. Tokens, identity headers, OpenAPI credentials and sensitive tool data
must not be written to ordinary logs.
